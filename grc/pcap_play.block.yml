# auto-generated by grc.converter

id: pcap_play
label: Pcap Play
category: '[pcap]'

parameters:
-   id: f
    label: Filename
    dtype: string
    default: /tmp/gnuradio.pcap
-   id: s
    label: Timescale
    dtype: float
    default: '1.0'

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import gnuradio.pcap as pcap
    make: pcap.pcap_play(${f}, ${s})

documentation: "This block plays pcap files back into a GNU Radio graph\n    timing\
    \ from the pcap headers is respected, \n    the timescale is a multiplier for\
    \ the timing used durring playback, i.e.\n    1.0 = realtime, 0.5 = 2x realtime,\
    \ etc"

file_format: 1
