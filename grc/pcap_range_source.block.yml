# auto-generated by grc.converter

id: pcap_range_source
label: Pcap Range Source
category: '[pcap]'

parameters:
-   id: f
    label: Filename
    dtype: string
    default: /tmp/gnuradio.pcap
-   id: fs
    label: Sample Rate (Hz)
    dtype: real
    default: 50e3

inputs:
-   domain: message
    id: pdus
    optional: true

outputs:
-   domain: message
    id: pdus
    optional: true

templates:
    imports: import gnuradio.pcap as pcap
    make: pcap.pcap_range_source(${f},${fs})

file_format: 1
